export type MapContextValue = {
  mapLib: MapLib | null;
  map: MapRef | null;
};

export const MapContext = React.createContext<MapContextValue>({ mapLib: null, map: null });

type MapInitOptions = Omit<
